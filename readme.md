# Motivation Bot - Telegram Bot на Node.js

Мотивационный Telegram бот, полностью переписанный с Python на Node.js. Бот отправляет мотивационные сообщения пользователям по расписанию и позволяет получать случайные мотивационные тексты.

## 🚀 Особенности

- ✅ Современный Node.js (ES modules)
- ✅ Чистая модульная архитектура
- ✅ Grammy - современная библиотека для Telegram Bot API
- ✅ SQLite для хранения данных пользователей
- ✅ JSON файлы вместо Google Sheets
- ✅ Автоматические напоминания по расписанию
- ✅ Готов к production deployment

## 📋 Требования

- Node.js 20+ (LTS)
- npm или yarn
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## 🛠️ Установка

1. **Клонируйте репозиторий** (если еще не склонирован)

2. **Установите зависимости:**
```bash
npm install
```

3. **Создайте файл `.env`** на основе `.env.example`:
```bash
cp .env.example .env
```

4. **Настройте переменные окружения** в `.env`:
```env
BOT_TOKEN=your_telegram_bot_token_here
MODE=dev
PORT=3000
DB_PATH=./data/bot.db
TEXTS_PATH=./data/texts.json
```

5. **Конвертируйте CSV в JSON** (если еще не сделано):
```bash
node scripts/convert-csv-to-json.js
```

## 🏃 Запуск

### Локальная разработка (Development)

Бот использует **long polling** для получения обновлений от Telegram:

```bash
npm run dev
```

Это запустит бота в режиме разработки с автоперезагрузкой при изменении файлов.

**Переменные окружения для разработки:**
```env
MODE=dev
BOT_TOKEN=your_token_here
```

### Production (Railway)

Для деплоя на Railway используйте:

```bash
npm start
```

**Переменные окружения для Railway:**
- `MODE=prod` (установите в Railway dashboard)
- `BOT_TOKEN=your_token_here` (установите в Railway dashboard)
- `PORT` - Railway автоматически устанавливает эту переменную

**Важно:** Бот использует **long polling**, а не webhook. Это идеально для Railway, так как процесс работает непрерывно.

## 📁 Структура проекта

```
motivation-bot/
├── src/
│   ├── index.js              # Точка входа (только bootstrap)
│   ├── bot.js                # Инициализация бота
│   ├── config/
│   │   └── env.js            # Загрузка переменных окружения
│   ├── handlers/
│   │   ├── commands.js       # Обработчики команд
│   │   ├── messages.js       # Обработчики сообщений
│   │   └── index.js
│   ├── services/
│   │   ├── database.js       # Работа с SQLite
│   │   ├── texts.js          # Работа с JSON текстами
│   │   └── scheduler.js      # Планировщик напоминаний
│   └── utils/
│       ├── keyboards.js      # Клавиатуры Telegram
│       └── constants.js      # Константы приложения
├── data/
│   ├── texts.json            # Мотивационные тексты (JSON)
│   └── bot.db                # База данных SQLite (создается автоматически)
├── scripts/
│   └── convert-csv-to-json.js # Конвертер CSV → JSON
├── .env                      # Переменные окружения (не в git)
├── .env.example              # Пример конфигурации
├── package.json
└── README.md
```

## 🎯 Команды бота

- `/start` - Начать работу с ботом
- `/random` - Получить случайную мотивацию
- `/useful` - Полезные материалы и ссылки
- `/motivation_type` - Описание типов поддержки
- `/motivation` - Мотивация по категориям
- `/change_motivation` - Сменить тип мотивации
- `/change_frequency` - Сменить частоту напоминаний
- `/reload_texts` - Обновить тексты из файла
- `/help` - Список всех команд

## ⚙️ Режимы работы

Бот поддерживает два режима работы через переменную окружения `MODE`:

### Development (dev)
- **Использование:** Локальная разработка
- **Запуск:** `npm run dev`
- **Особенности:**
  - Long polling для получения обновлений
  - Автоперезагрузка при изменении файлов
  - Подробное логирование
  - Не требует HTTPS

### Production (prod)
- **Использование:** Railway и другие production окружения
- **Запуск:** `npm start`
- **Особенности:**
  - Long polling для получения обновлений
  - Непрерывная работа процесса
  - Оптимизированное логирование
  - Railway совместимость

**Важно:** В обоих режимах используется **long polling**, а не webhook. Это означает:
- ✅ Не требуется HTTPS для локальной разработки
- ✅ Не требуется настройка webhook URL
- ✅ Процесс работает непрерывно (идеально для Railway)
- ✅ Один экземпляр бота (нет дублирования)

## 🔧 Архитектурные решения

### Почему Grammy вместо node-telegram-bot-api?

- ✅ Лучшая поддержка TypeScript и ES modules
- ✅ Более современная архитектура с middleware
- ✅ Встроенная поддержка сессий
- ✅ Лучшая производительность
- ✅ Активная разработка и поддержка

### Модульная архитектура

Проект разделен на четкие модули:
- **config/** - Конфигурация (env переменные)
- **handlers/** - Обработчики команд и сообщений
- **services/** - Бизнес-логика (БД, тексты, планировщик)
- **utils/** - Вспомогательные функции и константы

### Замена Google Sheets на JSON

Вместо интеграции с Google Sheets используется простой JSON файл (`data/texts.json`). Это:
- ✅ Быстрее (нет сетевых запросов)
- ✅ Проще в развертывании
- ✅ Не требует API ключей
- ✅ Легко редактировать и версионировать

## 📝 Обновление текстов

1. Отредактируйте `data/texts.json` вручную, или
2. Обновите CSV файл и запустите:
```bash
node scripts/convert-csv-to-json.js
```

## 🚢 Деплой

Бот готов к деплою на любой Node.js хостинг:

### Railway

1. **Подключите GitHub репозиторий** к Railway
2. **Добавьте переменные окружения** в Railway dashboard:
   - `BOT_TOKEN` - ваш Telegram bot token
   - `MODE=prod` - режим production
   - `PORT` - Railway устанавливает автоматически (не нужно настраивать)
3. **Railway автоматически:**
   - Определит Node.js проект
   - Запустит `npm install`
   - Запустит `npm start`
4. **Бот будет работать непрерывно** используя long polling

**Примечание:** Бот использует long polling, поэтому не требует webhook URL или HTTPS. Процесс работает как обычное Node.js приложение.

### Render
1. Создайте новый Web Service
2. Подключите репозиторий
3. Build Command: `npm install`
4. Start Command: `npm start`
5. Добавьте переменную окружения `BOT_TOKEN`

### Fly.io
```bash
fly launch
fly secrets set BOT_TOKEN=your_token
fly deploy
```

### VPS (через PM2)
```bash
npm install -g pm2
pm2 start src/index.js --name motivation-bot
pm2 save
pm2 startup
```

## 🔒 Безопасность

- ✅ Все секреты хранятся в `.env` (не коммитятся в git)
- ✅ `.env` добавлен в `.gitignore`
- ✅ Нет хардкода токенов в коде
- ✅ Используется dotenv для загрузки переменных

## 📊 База данных

Используется SQLite с двумя таблицами:
- `users` - данные пользователей (тип мотивации, частота, расписание)
- `sent_texts` - история отправленных текстов

База данных создается автоматически при первом запуске.

## 🐛 Отладка

Логи выводятся в консоль. Для production рекомендуется использовать:
- PM2 (с логами)
- Winston или Pino для структурированных логов
- Sentry для отслеживания ошибок

## 📄 Лицензия

MIT

## 🤝 Вклад

Проект мигрирован с Python на Node.js. Старый код находится в папке `old_bot_py/`.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте, что `.env` файл создан и содержит `BOT_TOKEN`
2. Убедитесь, что `data/texts.json` существует
3. Проверьте логи в консоли

---

**Примечание:** Этот бот полностью заменил Python версию. Все Google API интеграции удалены, используется только JSON для хранения текстов.
