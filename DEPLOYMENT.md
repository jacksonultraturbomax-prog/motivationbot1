# Инструкция по развертыванию

## 🏠 Локальная разработка

### Быстрый старт

1. **Установите зависимости:**
```bash
npm install
```

2. **Создайте `.env` файл:**
```bash
cp .env.example .env
```

3. **Настройте `.env`:**
```env
BOT_TOKEN=your_telegram_bot_token_here
MODE=dev
```

4. **Запустите бота:**
```bash
npm run dev
```

Бот будет работать в режиме разработки с автоперезагрузкой.

### Что происходит:
- ✅ Бот использует **long polling** для получения обновлений
- ✅ Автоматическая перезагрузка при изменении файлов
- ✅ Подробное логирование
- ✅ Остановка через `Ctrl+C`

---

## 🚂 Деплой на Railway

### Шаг 1: Подготовка репозитория

Убедитесь, что ваш код закоммичен и запушен в GitHub.

### Шаг 2: Создание проекта в Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий

### Шаг 3: Настройка переменных окружения

В Railway dashboard перейдите в **Variables** и добавьте:

| Переменная | Значение | Описание |
|-----------|----------|----------|
| `BOT_TOKEN` | `your_token_here` | Токен Telegram бота (обязательно) |
| `MODE` | `prod` | Режим production (обязательно) |
| `PORT` | *(автоматически)* | Railway устанавливает автоматически |

**Важно:** Не устанавливайте `PORT` вручную - Railway делает это автоматически.

### Шаг 4: Деплой

Railway автоматически:
1. Определит Node.js проект
2. Запустит `npm install`
3. Запустит `npm start`
4. Бот начнет работать

### Шаг 5: Проверка

1. Откройте вкладку **Logs** в Railway
2. Вы должны увидеть:
   ```
   ==================================================
   🚀 Запуск мотивационного бота [PRODUCTION MODE]
   📡 Режим: Long Polling (без webhook)
   ✅ Бот успешно запущен и готов к работе!
   ```

3. Проверьте бота в Telegram - отправьте `/start`

---

## 🔍 Проверка работы

### Локально:
```bash
# В консоли должно быть:
🚀 Запуск мотивационного бота [DEVELOPMENT MODE]
📡 Режим: Long Polling (без webhook)
✅ Бот успешно запущен и готов к работе!
```

### На Railway:
В логах Railway должно быть:
```
🚀 Запуск мотивационного бота [PRODUCTION MODE]
📡 Режим: Long Polling (без webhook)
✅ Бот успешно запущен и готов к работе!
```

---

## ❓ Часто задаваемые вопросы

### Q: Нужен ли HTTPS для локальной разработки?
**A:** Нет! Long polling не требует HTTPS. Webhook требует, но мы его не используем.

### Q: Нужно ли настраивать webhook URL в Railway?
**A:** Нет! Бот использует long polling, webhook не нужен.

### Q: Как остановить бота на Railway?
**A:** В Railway dashboard нажмите "Stop" в настройках сервиса.

### Q: Бот не отвечает после деплоя
**A:** Проверьте:
1. Правильность `BOT_TOKEN` в Railway variables
2. Логи в Railway dashboard на наличие ошибок
3. Что `MODE=prod` установлен

### Q: Можно ли использовать другой хостинг?
**A:** Да! Любой хостинг, поддерживающий долгоживущие Node.js процессы:
- Render (Web Service)
- Fly.io
- Heroku
- VPS с PM2

Просто установите `MODE=prod` и `BOT_TOKEN`.

---

## 🔧 Технические детали

### Long Polling vs Webhook

**Long Polling (используется):**
- ✅ Бот постоянно опрашивает Telegram API
- ✅ Не требует HTTPS
- ✅ Не требует публичного URL
- ✅ Идеально для Railway

**Webhook (не используется):**
- ❌ Требует HTTPS
- ❌ Требует публичный URL
- ❌ Сложнее настройка
- ❌ Не подходит для Railway без дополнительной настройки

### Почему один процесс?

Grammy `bot.start()` запускает long polling в текущем процессе. Это означает:
- Один экземпляр бота
- Нет дублирования сообщений
- Простое управление жизненным циклом

---

## 📝 Обновление бота на Railway

1. Закоммитьте изменения в GitHub
2. Railway автоматически обнаружит изменения
3. Запустится новый деплой
4. Старый процесс будет остановлен, новый запущен

---

## 🆘 Проблемы и решения

### Ошибка: "Отсутствуют обязательные переменные окружения"
**Решение:** Убедитесь, что `BOT_TOKEN` установлен в Railway variables.

### Ошибка: "Недопустимое значение MODE"
**Решение:** Установите `MODE=prod` (не `production`, не `PROD`).

### Бот запускается, но не отвечает
**Решение:** 
1. Проверьте логи Railway
2. Убедитесь, что токен правильный
3. Проверьте, что бот не заблокирован в Telegram

---

**Готово!** Ваш бот готов к работе локально и на Railway! 🎉
